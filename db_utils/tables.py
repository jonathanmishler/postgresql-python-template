from typing import List
import psycopg3
import pydantic

f"CREATE TABLE IF NOT EXISTS {schema.get()}

def sql_from_model(model: pydantic.main.ModelMetaclass) -> str:
    

def create_tables(cur: psycopg3.Cursor, models: List[pydantic.main.ModelMetaclass]):

    for model in models:
        
        cur.execute(
            """
                CREATE TABLE IF NOT EXISTS
            """
        )